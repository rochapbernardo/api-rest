spring:
  datasource:
    url: jdbc:postgresql://switchback.proxy.rlwy.net:20830/railway
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate